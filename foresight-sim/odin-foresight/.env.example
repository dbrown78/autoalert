# Copy to .env and fill in your values

# PostgreSQL connection (required for training and live prediction)
DATABASE_URL=postgresql://user:pass@localhost:5432/autoalert

# Model directory (default: ./model/saved)
MODEL_DIR=./model/saved

# FastAPI server settings
API_PORT=8001
API_HOST=0.0.0.0
LOG_LEVEL=INFO
